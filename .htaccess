# Vehicle Sampark Apache Rewrite Rules for Hostinger
<IfModule mod_rewrite.c>
    RewriteEngine On

    # Prevent infinite rewrite loops
    RewriteCond %{ENV:REDIRECT_STATUS} 200
    RewriteRule ^ - [L]

    # Custom 404 Page
    ErrorDocument 404 /404.php

    # 1. SERVE 404 FOR DIRECT .php ADMIN FILE REQUESTS IN BROWSER
    RewriteCond %{THE_REQUEST} \s/(login|dashboard|batches|profile|single_pdf|batch_pdf|api)\.php[\s\?] [NC]
    RewriteRule ^ - [R=404,L]

    # 2. ALLOWED INTERNAL CUSTOM ADMIN ROUTES
    RewriteRule ^admin-qr-login/?$ login.php [L,QSA]
    RewriteRule ^admin-qr-dashboard/?$ dashboard.php [L,QSA]
    RewriteRule ^admin-qr-batches/?$ batches.php [L,QSA]
    RewriteRule ^admin-qr-profile/?$ profile.php [L,QSA]
    RewriteRule ^admin-qr-api/?$ api.php [L,QSA]
    RewriteRule ^admin-qr-single-pdf/?$ single_pdf.php [L,QSA]
    RewriteRule ^admin-qr-batch-pdf/?$ batch_pdf.php [L,QSA]
</IfModule>
